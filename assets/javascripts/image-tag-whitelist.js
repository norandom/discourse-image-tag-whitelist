export default {
  name: 'dropcap',

  initialize() {
    setup(helper);
    ]);
  }
};


export function setup(helper) {
  helper.whiteList([ 'span.dropcaps',
                     'span.sclass'
                   ]);
